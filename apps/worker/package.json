{
  "name": "@stonktube/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --watch dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "backfill": "tsc && node dist/backfill-apr2026.js",
    "reanalyze": "tsc && node dist/reanalyze.js",
    "backfill-durations": "tsc && node dist/backfill-durations.js",
    "backfill-channel": "tsc && node dist/backfill-channel.js",
    "retranscribe-creator": "tsc && node dist/retranscribe-creator.js",
    "earliest-per-creator": "tsc && node dist/earliest-per-creator.js",
    "debug-tom-nash": "tsc && node dist/debug-tom-nash.js",
    "onboard-creator": "tsc && node dist/onboard-creator.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "@stonktube/db": "workspace:*",
    "@stonktube/pipeline": "workspace:*",
    "@stonktube/shared": "workspace:*",
    "bullmq": "^5.12.12",
    "dotenv": "^17.4.2",
    "mongoose": "^8.5.4",
    "pino": "^9.3.2",
    "yahoo-finance2": "^3.15.3",
    "youtube-transcript": "^1.2.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.14",
    "typescript": "^5.5.4"
  }
}
