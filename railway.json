{
  "$schema": "https://railway.app/railway.schema.json",
  "services": {
    "web": {
      "buildCommand": "pnpm install && pnpm turbo run build --filter=api... --filter=web...",
      "startCommand": "node apps/api/dist/index.js",
      "healthcheckPath": "/healthz"
    },
    "worker": {
      "buildCommand": "pnpm install && pnpm turbo run build --filter=worker...",
      "startCommand": "node apps/worker/dist/index.js"
    },
    "scheduler": {
      "buildCommand": "pnpm install && pnpm turbo run build --filter=scheduler...",
      "startCommand": "node apps/scheduler/dist/index.js",
      "cronSchedule": "0 */4 * * *"
    }
  }
}
